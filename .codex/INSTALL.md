# Installing Embedded Memory for Codex

Fetch and follow instructions from:
`https://raw.githubusercontent.com/<OWNER>/<REPO>/main/.codex/INSTALL.md`

## Setup (example)

```bash
git clone https://github.com/<OWNER>/<REPO>.git ~/.codex/embedded-memory
```

Add the skill to your `~/.codex/AGENTS.md` (example):

```markdown
# Embedded Memory

Use this skill:
- embedded_memory:using_embedded_memory
```

In Codex, point the skill path to:
- `~/.codex/embedded-memory/skills/using_embedded_memory/SKILL.md`

